# Active repository mission

The current task is the complete Android R2 audit-correction mission in:

`ANDROID_R2_AUDIT_CORRECTION_MISSION.md`

Work only in `/srv/RSVP_reader-r2` on the existing remote branch:

`mission/android-r2-audit-corrections-20260813`

Read the mission completely before changing implementation. Preserve the useful inherited Android R1 commits, but treat every independent audit finding as a failing requirement until reproduced, corrected, and verified.

Continue autonomously through official free server toolchain setup, implementation corrections, clean builds, real API 36 phone/tablet emulator QA, truthful evidence, logical commits, and final push. Do not stop after planning, after installing the SDK, after Gradle compilation, or after generating screenshots.

Do not work in `/srv/RSVP_reader`. Do not deploy, merge, publish, production-sign, create a PR, use paid services, or trigger GitHub-hosted Actions.
