# Active repository mission

The current task is the complete Android R3 truthful server-proof mission in:

`ANDROID_R3_SERVER_PROOF_MISSION.md`

Work only through SSH on the canonical server in `/srv/RSVP_reader-r2` on:

`mission/android-r3-server-proof-20260813`

Read the mission completely before changing implementation. Preserve the real
R2 progress inherited at `51f735bd4a14786c5c94b40aa88f667f0540eb2a`, but
treat every R3 audit finding as failing until reproduced, fixed, and verified by
an actually executed check.

Continue autonomously through official free Linux toolchain setup, hermetic
tests, fail-closed evidence refactoring, clean remote-SHA builds, real API 36
phone/tablet platform QA, truthful screenshots/accessibility proof, durable
tester artifacts, final validation, and final push. Do not stop after planning,
after installing dependencies, or after compiling.

Never hard-code a `PASSED` result, trust a previous evidence JSON as current
proof, relabel repeated screenshots, or report a push/artifact/tool that does
not exist on the canonical server.

Do not work in `/srv/RSVP_reader`. Do not deploy, merge, publish, production-
sign, create a PR, use paid services, or trigger GitHub-hosted Actions.
